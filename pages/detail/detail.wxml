<view class='content'>
  <view class=''>
    <image src="{{movie.images.medium}}" mode='aspectFit' />
  </view>
  <view class='hr' />

  <view class='detail'>
    <text>{{movie.text}}</text>
  </view>

  <view class='casts'>
    <block wx:for="{{movie.casts}}" wx:key="">
      <image src='{{item.avatars.medium}}' mode='aspectFit' />
    </block>
  </view>
  <view class='hr' />

  <view class='detail'>
    <text>{{movie.summary}}</text>
  </view>
</view>